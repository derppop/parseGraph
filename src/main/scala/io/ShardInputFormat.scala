package io

class ShardInputFormat {
  // creates inputSplit(shard) instances
  // implements getSplits - reads file and creates inputSplit for each shard
  // implements createRecordReader - creates instance of record reader
}
